# Core (required)
websockets>=12.0
numpy>=1.24.0
pyyaml>=6.0
aiohttp>=3.9.0

# STT - offline speech-to-text (install for voice input)
faster-whisper>=1.0.0

# TTS - offline text-to-speech (optional, macOS/Linux have system TTS fallback)
# piper-tts>=1.2.0

# Optional features
sounddevice>=0.4.6
# asyncio-mqtt>=0.16.0
# edge-tts>=6.1.0
